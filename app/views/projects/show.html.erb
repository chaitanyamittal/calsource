<h3><%= @project.name if @project.name %></h3>
<p><%= @project.description %></p>
<h5>Team Members</h5>
<p><%= User.find(@project.user_id)%></p>

<% if @project.user == current_user %>
	<%= button_to "Delete", delete_path, :class => "button medium", :method => :path %>
<% end %>

<%= tweet_button %>
<%= pinit_button %>
<%= google_plus_button %>